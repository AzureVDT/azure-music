const LoadingSpinner = () => {
    return (
        <div
            className={`w-[24px] h-[24px] border-text1 
            border-4 border-t-transparent border-dotted rounded-full animate-spin inline-block`}
        ></div>
    );
};

export default LoadingSpinner;
